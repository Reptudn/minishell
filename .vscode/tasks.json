{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "minishell prelaunch",
            "command": "make debug",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}